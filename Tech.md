# AI 协作系统技术方案

## 项目概述

### 产品定位
面向项目管理的 AI 协作平台，支持需求输入、AI 多轮内部讨论、方案生成与对比选择，并提供历史记录管理和上下文复用功能。

### 核心功能
- 🎯 **需求分析**: 支持自然语言需求输入，AI 智能解析
- 🤖 **智能协作**: AI Agent 多轮内部讨论，生成多套方案
- 📊 **方案对比**: 可视化方案展示，支持并列对比
- 📚 **上下文管理**: 历史记录存储，上下文选择与压缩
- 🔧 **配置灵活**: 讨论轮数、方案数量等参数可调

## UI 设计方案

### 整体布局
基于现有 chat 页面的设计模式，采用左右两栏布局：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AI 协作页面 (h-screen)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│  左侧历史记录区        │                   右侧主工作区                        │
│  (可收起展开)          │                                                     │
│  ┌─────────────────┐   │  ┌─────────────────────────────────────────────────┐ │
│  │   新建协作      │   │  │              需求输入区                         │ │
│  │   [+ 新建]      │   │  │  ┌─────────────────────────────────────────┐   │ │
│  ├─────────────────┤   │  │  │        需求描述输入框                   │   │ │
│  │                 │   │  │  │   (复用 ChatInput 组件)                │   │ │
│  │   历史记录列表  │   │  │  └─────────────────────────────────────────┘   │ │
│  │   ┌───────────┐ │   │  │  ┌─────────────┐  ┌─────────────┐             │ │
│  │   │ 项目A需求 │ │   │  │  │ 讨论轮数: 1 │  │ 方案数量: 3 │             │ │
│  │   │ 2024-09-29│ │   │  │  └─────────────┘  └─────────────┘             │ │
│  │   └───────────┘ │   │  └─────────────────────────────────────────────────┘ │
│  │   ┌───────────┐ │   │                                                     │
│  │   │ 功能B设计 │ │   │  ┌─────────────────────────────────────────────────┐ │
│  │   │ 2024-09-28│ │   │  │                AI 方案展示区                    │ │
│  │   └───────────┘ │   │  │                                                 │ │
│  │       ...       │   │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────┐  │ │
│  │                 │   │  │  │   方案 A    │  │   方案 B    │  │  方案 C │  │ │
│  │   上下文选择    │   │  │  │  ┌───────┐  │  │  ┌───────┐  │  │ ┌─────┐ │  │ │
│  │   ☑ 项目A需求   │   │  │  │  │详细内容│  │  │  │详细内容│  │  │ │内容 │ │  │ │
│  │   ☐ 功能B设计   │   │  │  │  │优势劣势│  │  │  │优势劣势│  │  │ │优劣 │ │  │ │
│  │                 │   │  │  │  │评分难度│  │  │  │评分难度│  │  │ │评分 │ │  │ │
│  └─────────────────┘   │  │  └─────────────┘  └─────────────┘  └─────────┘  │ │
│  宽度: 280px (展开)     │  │                                                 │ │
│        70px (收起)      │  │  [选择方案 A]  [选择方案 B]  [选择方案 C]        │ │
│                         │  └─────────────────────────────────────────────────┘ │
│                         │                   flex-1 自适应                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 组件设计详情

#### 1. 左侧历史记录区 (HistorySidebar)
**基于 `CollapsibleSidebar` 组件实现**
- **展开宽度**: 280px (与 chat 页面 SideBar 保持一致)  
- **收起宽度**: 70px (显示图标按钮)
- **功能模块**:
  - 新建协作按钮 (类似 chat 的 "New Chat")
  - 历史记录列表 (按日期分组，复用 `Dropdown` 组件逻辑)
  - 上下文选择器 (多选框，选择历史会话作为上下文)
  - 搜索功能 (可选)

#### 2. 右侧主工作区 (MainWorkspace)
**采用垂直布局，分为两个主要区域**

##### 2.1 需求输入区 (RequirementInput)
- **组件复用**: 直接使用 `ChatInput` 组件
- **配置面板**: 两个数字输入器
  - 讨论轮数选择器 (默认 1，范围 1-5)
  - 方案数量选择器 (默认 3，范围 1-5)
- **提交按钮**: "开始 AI 协作" 按钮

##### 2.2 AI 方案展示区 (SchemeCanvas)
- **布局**: 响应式网格布局 (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)
- **方案卡片**: 每个方案一个独立卡片
  - 方案标题和描述
  - 优势/劣势列表
  - AI 评分和实施难度
  - 预估工期
  - 选择按钮
- **对比模式**: 支持并排对比多个方案
- **加载状态**: 使用 `Skeleton` 组件显示生成中状态

### 交互设计

#### 状态管理

**全局状态管理 (`valtioTool.ts`)**
使用项目内置的 `packages/hooks/src/valtioTool.ts` 工具进行状态管理：

```tsx
// aiCollabStore.ts - 全局业务状态
import { createProxy } from 'hooks'

export const aiCollabStore = createProxy({
  // 业务状态 - 需要跨组件共享
  currentSession: null as CollaborationSession | null,
  isGenerating: false,
  generationProgress: 0,
  
  // 历史记录状态 - 需要持久化和共享
  historyList: [] as CollaborationSession[],
  selectedHistoryId: null as string | null,
  selectedContextIds: [] as string[],
  
  // 配置状态 - 需要在会话间保持
  discussionRounds: 1,
  schemeCount: 3,
})

// 使用示例
function AiCollaborationPage() {
  const store = aiCollabStore.use()
  
  return (
    <div>
      {store.isGenerating && <LoadingSpinner />}
    </div>
  )
}
```

**状态管理策略**
- ✅ **全局状态**: 跨组件需要共享的业务数据 (会话、历史、配置)
- ✅ **类型安全**: 完整的 TypeScript 类型支持

#### 用户流程
1. **进入页面**: 显示历史记录列表，右侧显示空状态
2. **新建协作**: 点击"新建协作"按钮，右侧显示需求输入区
3. **输入需求**: 在输入框中描述需求，调整配置参数
4. **选择上下文**: (可选) 从左侧选择历史会话作为上下文
5. **开始协作**: 点击提交，AI 开始生成方案，显示加载动画
6. **查看方案**: 方案生成完毕，以卡片形式展示多个方案
7. **对比选择**: 可以对比不同方案，最终选择一个方案
8. **保存记录**: 自动保存到历史记录，可在左侧查看

#### 响应式设计
- **桌面端** (≥1024px): 左右两栏布局，历史区固定宽度
- **平板端** (768px-1023px): 历史区可收起，方案区 2 列网格
- **移动端** (<768px): 历史区浮层模式，方案区单列布局

### 技术实现要点

#### 组件复用策略
- **CollapsibleSidebar**: 左侧历史记录区的收起展开功能
- **ChatInput**: 需求输入组件，保持一致的用户体验
- **Button**: 各种操作按钮，使用统一的设计系统
- **Card**: 方案展示卡片，可能需要扩展现有 Card 组件
- **Dropdown**: 历史记录分组展示
- **Switch/Checkbox**: 配置选项和上下文选择

#### 动画效果
- **页面进入**: 使用 `framer-motion` 的 `motion.div`，参考 chat 页面动画
- **侧边栏收起展开**: 复用 `CollapsibleSidebar` 的内置动画
- **方案生成**: 骨架屏加载动画，渐入效果
- **方案对比**: 卡片悬浮和选中状态动画

#### 数据持久化
- **LocalForage**: 存储历史记录到 IndexedDB
- **会话恢复**: 页面刷新后恢复当前会话状态
- **上下文压缩**: 预留接口，支持历史会话内容压缩

### 设计亮点

1. **一致性**: 与现有 chat 页面保持视觉和交互一致性
2. **可扩展性**: 预留了 AI 讨论过程展示、方案评估等高级功能接口
3. **用户体验**: 支持键盘快捷键、拖拽排序、批量操作等高级交互
4. **性能优化**: 虚拟滚动、懒加载、组件 memo 化
5. **可访问性**: 支持屏幕阅读器、键盘导航、高对比度模式

## 技术架构

### 前端技术栈
| 技术 | 版本 | 作用 |
|------|------|------|
| React | 18+ | 前端框架 |
| TypeScript | 5+ | 类型系统 |
| Vite | 5+ | 构建工具 |
| TailwindCSS | 3+ | 样式框架 |
| Framer Motion | 11+ | 动画库 |
| LocalForage | 1.10+ | 本地存储 |
| React Router | 6+ | 路由管理 |
| Valtio | - | 状态管理 |

### 后端技术栈 (预留)
| 技术 | 作用 | 备注 |
|------|------|------|
| Node.js + NestJS | API 服务 | 可替换为其他技术栈 |
| OpenAI API / 其他 LLM | AI 能力 | 支持多模型切换 |
| PostgreSQL | 数据存储 | 按需选择 |

### 系统架构图
```
┌─────────────────────────────────────────────────────────────┐
│                        前端应用层                            │
├─────────────────────────────────────────────────────────────┤
│  需求输入区    │      方案展示画布      │    历史记录区     │
│  - ChatInput   │   - AgentPanel        │   - HistoryList   │
│  - ConfigPanel │   - SchemeComparison  │   - ContextSelect │
├─────────────────────────────────────────────────────────────┤
│                       业务逻辑层                            │
│  useAiCollab   │  useHistoryManager   │   useSchemeGen    │
├─────────────────────────────────────────────────────────────┤
│                       数据存储层                            │
│    LocalForage     │      API 接口层      │    状态管理     │
│   (IndexedDB)      │    (预留扩展)        │   (useGetState)  │
└─────────────────────────────────────────────────────────────┘
```

## 核心数据结构

### 协作会话类型
```typescript
/**
 * AI 协作会话
 */
export type CollaborationSession = {
  /** 会话 ID */
  id: string
  /** 会话标题 */
  title: string
  /** 需求描述 */
  requirement: string
  /** 会话配置 */
  config: SessionConfig
  /** 生成的方案列表 */
  schemes: Scheme[]
  /** AI 讨论过程 */
  discussions: AgentDiscussion[]
  /** 选中的方案 ID */
  selectedSchemeId?: string
  /** 创建时间 */
  createdAt: number
  /** 更新时间 */
  updatedAt: number
  /** 会话标签 */
  tags?: string[]
}

/**
 * 会话配置
 */
export type SessionConfig = {
  /** 讨论轮数 @default 1 */
  discussionRounds: number
  /** 方案数量 @default 3 */
  schemeCount: number
  /** 上下文来源会话 ID 列表 */
  contextSessionIds: string[]
}

/**
 * 方案对象
 */
export type Scheme = {
  /** 方案 ID */
  id: string
  /** 方案标题 */
  title: string
  /** 方案描述 */
  description: string
  /** 详细方案内容 */
  content: string
  /** 方案优势 */
  pros: string[]
  /** 方案劣势/风险 */
  cons: string[]
  /** 实施难度 (1-5) */
  difficulty: number
  /** 预估工期 (天) */
  estimatedDays: number
  /** AI 评分 (1-100) */
  aiScore: number
  /** 相关资源需求 */
  resources?: string[]
}
```

### UI 组件架构
```
AiCollaborationPage/
├── components/
│   ├── RequirementInput/          # 需求输入区
│   │   ├── RequirementForm.tsx
│   │   └── ConfigPanel.tsx
│   ├── SchemeCanvas/              # 方案展示区  
│   │   ├── SchemeList.tsx
│   │   ├── SchemeCard.tsx
│   │   ├── SchemeComparison.tsx
│   │   └── DiscussionViewer.tsx
│   ├── HistoryDrawer/             # 历史记录区
│   │   ├── HistoryList.tsx
│   │   ├── HistoryItem.tsx
│   │   ├── ContextSelector.tsx
│   │   └── SearchBar.tsx
│   └── shared/                    # 共享组件
├── hooks/
│   ├── useAiCollab.ts            # 核心业务逻辑
│   ├── useHistoryManager.ts      # 历史记录管理
│   ├── useSchemeGenerator.ts     # 方案生成
│   └── useContextManager.ts      # 上下文管理
├── types.ts                      # 类型定义
├── constants.ts                  # 常量配置
└── index.tsx                     # 页面入口
```

## 开发工期评估

### 按阶段分解 (按 1 人开发计算)

| 阶段    | 工作内容                  | 预估工期 | 开始日期  | 结束日期  |
| ----- | --------------------- | ---- | ----- | ----- |
| 架构与基础 | 技术方案、数据结构设计、项目结构、类型定义 | 1 人日 | Day 1 | Day 1 |
| UI 实现 | 三栏布局、核心组件             | 2 人日 | Day 2 | Day 3 |
| 数据层开发 | 状态管理、本地存储             | 待定   | 待定    | 待定    |
| 接口预留  | 假接口、模拟数据              | 1 人日 | Day 4 | Day 4 |
| 集成测试  | 功能集成、流程测试             | 1 人日 | Day 5 | Day 5 |
| 优化完善  | 动画、响应式、性能             | 2 人日 | Day 6 | Day 7 |

**总计**: 11 人日 (约 2.2 周)

## 资源需求

### 人力资源
- **前端开发**: 1 人 (全栈能力，熟悉 React/TypeScript)
- **UI/UX 支持**: 0.5 人 (兼职，提供设计规范和交互建议)
- **产品需求**: 0.2 人 (需求澄清、验收测试)

### 技术资源
- **开发环境**: 已具备 (React + Vite + TailwindCSS 环境)
- **设计系统**: 已具备 (项目现有组件库和设计 Token)
- **AI 能力**: 暂未集成 (预留接口，后续对接)

### 风险评估
| 风险点 | 风险等级 | 应对方案 |
|--------|----------|----------|
| AI 接口对接复杂度 | 中 | 先用模拟数据，预留标准化接口 |
| 复杂状态管理 | 中 | 采用 `useGetState` 避免闭包陷阱 |
| 移动端适配 | 低 | 使用 TailwindCSS 响应式设计 |
| 性能问题 | 低 | React.memo |

## 后续扩展规划

### Phase 2: 后端集成
- AI 模型接入 (OpenAI/Claude/本地模型)
- 用户系统和权限管理
- 云端数据同步

### Phase 3: 高级功能  
- 协作会话分享
- 方案模板系统
- 数据分析和统计
- 批量处理和自动化
