## 上下文
阅读 packages/app/src/router/index.tsx 了解路由结构，我需要新建一个路由页面，你在 packages/app/src/views/aiCollaboration 里面创建
这个页面功能是集成 AI 协作
- packages/hooks 可用 hook 包
- packages/utils 工具包
- packages/app/src/components 可用组件

## 背景
产品经理需要实现一套集成 AI 协作的页面，主要是面向项目管理
比如说他会提出某个需求，然后让 AI 进行方案制定，这里面可能包含多轮 AI Agent 内部自己讨论出多个方案，然后挑选几个最佳方案以供选择
这个讨论几轮、几个方案是可以配置的。初版我们仅仅做一轮即可，我们的目标是先做出功能
然后这里可以保存到历史记录，这里你先用模拟函数调用，函数内部就用 localforage 存到 IndexDB 即可，并且打上 @TODO 标记，方便我后续修改更新
每一个历史记录作为一项，存起来先，后续提供上下文压缩功能，可以提取精炼某个对话。这些上下文可以选择加入某个对话中，作为上下文
这一套主要是为了让 AI 能够快速了解当前的环境、目标，并且快速给出方案

## 注意
你需要先搭建好基础的 UI 结构，然后我再进行后续的开发
注意留好接口的开放性，方便我后续修改更新，你可以用假函数，打上 @TODO 标记，方便我后续修改更新

在 PLAN.md，说明你的计划，每个计划都用 TODO 标记，后续完成每个计划，你就更新计划进度，就让我验收，层层递进