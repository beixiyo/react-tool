# AI 协作页面开发计划

## 项目分析阶段
- [x] ~~阅读 `packages/app/src/router/index.tsx`(了解路由结构，注意路由会扫描 `src/views/**/index.tsx`，所以你的组件名称结构要注意)、`tailwind.config.mjs`、`tsconfig.json`，确认路由扫描规则、路径别名与设计 Token~~
- [x] ~~盘点 `packages/app/src/components`、`packages/hooks`、`packages/utils` 中可复用的能力，参考现有 `chat` 页面的实现~~
- [x] ~~分析现有智能体对话系统的数据结构和交互模式（`ChatMessage`、`Agent`、`AgentStep` 等类型）~~

## 架构设计阶段  
- [x] 编写技术方案文档 `Tech.md`（包含前后端方案、工期评估、资源需求）
- [x] 设计 AI 协作核心流程：需求输入 → AI 分析 → 方案生成 → 多轮讨论展示 → 方案选择的完整 UX 流程
  - `阶段 0` 初始：加载最近一次会话，左栏展示需求输入表单与配置面板；中栏在初次协作时展示灰度插画 + 三行以内引导文案，并提供「知道了，不再显示」轻量入口（本地缓存一次性）；右栏折叠的历史抽屉可展开预览。
  - `阶段 1` 需求输入：用户填写需求描述、目标、上下文选择并点击「发起协作」。提交时生成 `CollaborationSession`，创建本地占位会话卡片并进入分析态，左栏锁定只允许补充备注。
  - `阶段 2` AI 分析：中栏展示 AI 解析进度条与分析摘要（需求拆解、关键约束、风险提示）。实时推送分析片段追加到时间线，右栏同步记录该会话的分析版本。
  - `阶段 3` 方案生成：根据配置生成多套 `PlanCandidate`，以卡片栅格呈现，内含解决思路、实施步骤、资源评估与风险；支持在卡片上请求「进一步细化」「调整约束」。
  - `阶段 4` 多轮讨论：每个方案都维持独立对话流，沿用 `AgentStep` 展示用户与 AI 的往返消息，可在方案卡片侧栏查看讨论纪要并追加追问或反馈。
  - `阶段 5` 方案选择：用户在卡片上标记「首选」或「备选」，触发总结弹窗汇总关键指标；确认后将结果写入会话历史并提供导出（复制、生成任务单、同步其他系统等）入口。

## 基础开发阶段
- [x] ~~定义数据结构：协作会话、方案对象、历史记录、上下文压缩等的 TypeScript 类型定义~~
- [x] ~~完善 `packages/app/src/views/aiCollaboration` 目录结构：~~
  - [x] ~~`index.tsx` - 页面入口~~
  - [x] ~~`types.ts` - 类型定义~~  
  - [x] ~~`constants.ts` - 常量配置~~
  - [x] ~~`hooks/useAiCollab.ts` - 核心业务逻辑 hook~~
  - [x] ~~`hooks/useHistoryManager.ts` - 历史记录管理 hook~~
  - [x] ~~`components/` - 页面组件~~
- [x] ~~定义页面配置类型（讨论轮数、方案数量、上下文选择等），确保通过 props 可控，补充 JSDoc~~

## UI 实现阶段
- [x] ~~搭建响应式两栏布局：~~
  - [x] ~~左侧：历史记录侧栏（可收起展开，基于 `CollapsibleSidebar`）~~
  - [x] ~~右侧：需求输入区 & AI 方案展示画布（支持多方案对比）~~
- [x] ~~实现核心 UI 组件：~~
  - [x] ~~需求输入组件（复用 `Textarea`）~~
  - [x] ~~方案展示组件（基础方案卡片）~~
  - [x] ~~配置面板组件（已移除，由 AI 自动决定讨论轮数和方案数量）~~
  - [ ] AI 进度展示组件（在方案区域内部，实时滚动显示进度和日志）
- [ ] 历史记录列表组件（当前为占位符）

## 数据层实现阶段  
- [x] ~~实现基于 `localforage` 的历史记录存储：~~
  - [x] ~~保存/读取协作会话记录~~
  - [x] ~~上下文数据管理~~
  - [ ] 数据索引和搜索功能（后续扩展）
- [x] ~~实现状态管理逻辑：~~
  - [x] ~~当前会话状态（基于 `valtioTool`）~~
  - [x] ~~AI 生成状态管理~~
  - [x] ~~UI 交互状态~~

## 接口预留阶段
- [x] ~~暴露扩展接口占位（标记 `@TODO`）：~~
  - [x] ~~AI 方案生成接口（已在 `useAiCollab.ts` 中预留）~~
  - [x] ~~多轮 Agent 对话接口（已在 `types.ts` 中定义）~~  
  - [x] ~~上下文压缩接口（已在 `useHistoryManager.ts` 中标记）~~
  - [x] ~~方案评估和打分接口（已在 `types.ts` 中定义）~~
- [x] ~~实现模拟数据和假函数，确保 UI 流程完整可演示（详见 `mocks.ts`）~~

## 优化完善阶段
- [x] ~~响应式适配和暗色模式支持（已实现基础响应式和暗色模式样式）~~
- [x] ~~动画和交互细节优化（使用 `framer-motion`）~~
- [x] ~~性能优化（组件 `memo` 化、虚拟滚动等）~~
- [ ] 错误处理和边界情况（需要进一步完善）

## 待完善功能（下一阶段）
- [ ] AI 进度展示组件（`GenerationProgress.tsx`）
  - 进度条和百分比显示
  - 实时日志滚动动画
  - 骨架屏占位布局
- [x] ~~上下文选择组件（`ContextSelector.tsx`）~~
  - ~~多选复选框交互~~
  - ~~展开/收起动画~~
  - ~~上下文信息展示~~
- [ ] 历史记录列表组件的具体实现
- [ ] 数据索引和搜索功能
- [ ] 完善错误处理和边界情况
- [ ] 集成真实的 AI 接口（替换模拟数据）
